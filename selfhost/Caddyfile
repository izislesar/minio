# laptop loopback is enough for a backup target. if you ever expose it:
# s3 with path-style + tls, console stays loopback-only. not configured here
# on purpose — exposing an s3 endpoint is a decision, not a default.
